# GitHub認証エラー解決方法

`git push` が止まってしまう場合の解決方法です。

## 🔴 問題の原因

GitHubは2021年8月からパスワード認証を廃止しました。
そのため、`git push` でパスワードを入力しても認証できません。

## ✅ 解決方法：Personal Access Tokenを使用

### ステップ1: Personal Access Tokenを作成

1. **GitHubにログイン**
   - https://github.com にアクセス

2. **Settingsに移動**
   - 右上のプロフィール画像をクリック
   - 「Settings」を選択

3. **Developer settingsに移動**
   - 左サイドバーの一番下「Developer settings」をクリック

4. **Personal access tokens → Tokens (classic)**
   - 「Personal access tokens」をクリック
   - 「Tokens (classic)」を選択

5. **新しいトークンを生成**
   - 「Generate new token」→「Generate new token (classic)」をクリック

6. **トークンの設定**
   - **Note**: `Streamlit app deployment` など、わかりやすい名前を入力
   - **Expiration**: お好みで設定（90日、無期限など）
   - **Select scopes**: `repo` にチェック（すべてのリポジトリへのアクセス）
   - 「Generate token」をクリック

7. **トークンをコピー**
   - ⚠️ **重要**: この画面でしかトークンは表示されません！
   - トークンをコピーして安全な場所に保存

### ステップ2: 現在のプッシュをキャンセル

ターミナルで `Ctrl + C` を押して、現在のコマンドをキャンセルしてください。

### ステップ3: 再度プッシュ（トークンを使用）

```bash
git push -u origin main
```

**認証プロンプトが表示されたら**:
- **Username**: GitHubのユーザー名（`lube1612`）を入力
- **Password**: パスワードの代わりに、**コピーしたPersonal Access Token**を貼り付け
  - ⚠️ トークンを貼り付けても画面には何も表示されません（セキュリティのため）
  - Enterキーを押す

### ステップ4: 成功を確認

成功すると、以下のようなメッセージが表示されます：

```
Enumerating objects: XX, done.
Counting objects: 100% (XX/XX), done.
Writing objects: 100% (XX/XX), done.
To https://github.com/lube1612/mercari-scraping-app.git
 * [new branch]      main -> main
Branch 'main' set up to track remote branch 'main' from 'origin'.
```

---

## 🔄 別の方法：Git Credential Managerを使用

### macOSの場合

```bash
# Git Credential Managerをインストール（Homebrewを使用）
brew install git-credential-manager

# 設定
git config --global credential.helper osxkeychain
```

その後、再度 `git push` を実行すると、ブラウザが開いて認証できます。

---

## 🛠️ トラブルシューティング

### 問題1: トークンを入力しても認証できない

**確認事項**:
- トークンを正しくコピーしたか（余分なスペースがないか）
- `repo` スコープが選択されているか
- トークンの有効期限が切れていないか

### 問題2: リモートURLが間違っている

**確認方法**:
```bash
git remote -v
```

**修正方法**:
```bash
git remote set-url origin https://github.com/lube1612/mercari-scraping-app.git
```

### 問題3: リモートが既に存在する

**確認方法**:
```bash
git remote -v
```

**修正方法**:
```bash
git remote remove origin
git remote add origin https://github.com/lube1612/mercari-scraping-app.git
```

---

## 📝 今後のために：認証情報を保存

一度認証が成功すると、macOSのキーチェーンに保存されます。
次回からは自動的に認証されます。

---

## ✅ 確認方法

プッシュが成功したら、以下で確認：

1. **GitHubリポジトリにアクセス**
   ```
   https://github.com/lube1612/mercari-scraping-app
   ```

2. **確認項目**:
   - ✅ `streamlit_app.py` が表示されている
   - ✅ `requirements.txt` が表示されている
   - ✅ ブランチが `main` になっている

3. **Streamlit Cloudで確認**
   - Streamlit Cloudの画面を更新（F5）
   - リポジトリを選択
   - エラーが消えているか確認
